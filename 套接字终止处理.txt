套接字终止处理
通常使用close函数终止一个连接，但是该函数有如下两点限制：
1，close函数知识减少描述符的参考数，并不直接关闭连接。只有当ref数变为0的时候，系统才进行关闭连接的操作，但是在某些情况下，需要直接关闭连接而不考虑描述符ref的值
2，close函数将终止双向的数据传输，即终止读和写操作，由于TCP连接是全双工的，在某些情况下只需要关闭一个方向的数据传输

为避免上述问题，可采用shutdown函数来关闭连接
shutdown函数可直接关闭套接字也不考虑其ref值是否为0, 并且它可以选择仅一个方向的连接，另一个方向可以继续工作直到需要关闭




